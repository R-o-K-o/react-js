import css from './HomePage.module.css';

export const HomePage = () => {
    return (
        <div className={css.home_page}>
            <h1 className={css.title}>the rick and morty</h1>
        </div>
    );
};